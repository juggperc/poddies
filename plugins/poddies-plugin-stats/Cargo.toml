[package]
name = "poddies-plugin-stats"
version = "0.1.0"
edition = "2021"
description = "Listening statistics panel for Poddies."
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
poddies-plugin-sdk = { path = "../../crates/poddies-plugin-sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
